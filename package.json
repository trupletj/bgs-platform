{
  "name": "bgs-platform",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "mobile": "pnpm --filter mobile",
    "attendance": "pnpm --filter attendance"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  },
  "packageManager": "pnpm@11.0.8",
  "engines": {
    "node": ">=20"
  }
}
